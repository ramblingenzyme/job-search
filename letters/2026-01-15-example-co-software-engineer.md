---
role: Software Engineer
company: Example Co
greeting: "Hi Example Co team,"
# signoff: defaults to "Kind regards,"
# signature: defaults to "Your Name"
---

Markdown body. Each blank-line-separated paragraph becomes a paragraph in the
letter; the heading, contact details and sign-off come from the template.

The date in the header comes from this file's name, not the frontmatter, and
the output PDF is named from `company` and `role` above.

Delete this file and run `./new.sh "Some Company" "Some Role"` to start your own.
